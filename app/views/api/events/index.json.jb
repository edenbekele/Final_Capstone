@events.map do |event|
  {
    id: event.id,
    name: event.name,
    date: event.date.gsub("-", "/"),
    location: event.location,
    description: event.description,
    image: event.image,
  }
end
